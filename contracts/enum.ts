export enum MaritalStatus {
  SINGLE = "single",
  MARRIED = "married",
  SAPERATED = "saperated",
}

export enum Gender {
  MALE = "male",
  FEMALE = "female",
}

export enum ImageType {
  SINGLE = "single",
  GROUP = "group",
}

export enum PasswordStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
}

export enum UserType {
  USER = "user",
  ADMIN = "admin",
}
